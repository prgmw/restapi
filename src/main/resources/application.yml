spring:
  application:
    name: restapi
  server:
    port: 8080
  data:
    mongodb:
      host: '${MONGO_HOST}'
      authentication-database: '${MONGO_DB}'
      database: '${MONGO_DB}'
      port: 27017
      username: '${MONGO_USERNAME}'
      password: '${MONGO_PASSWORD}'
